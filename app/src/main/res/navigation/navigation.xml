<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@+id/calendarFragment">

    <fragment
        android:id="@+id/calendarFragment"
        android:name="com.willy.metu.calendar.CalendarFragment"
        android:label="CalendarFragment"
        tools:layout="@layout/fragment_calendar"/>

    <dialog
        android:id="@+id/calendarBottomSheet"
        android:name="com.willy.metu.calendar.CalendarBottomSheet"
        android:label="CalendarBottomSheet"
        tools:layout="@layout/bottom_sheet_calendar"/>

    <action android:id="@+id/navigate_to_calendar_bottom_sheet"
        app:destination="@id/calendarBottomSheet"/>

</navigation>